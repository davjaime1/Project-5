The website was hosted locally on: http://localhost:8000/home
The website does the same as before. Each page is dynamic pulling data from the database. 
There two users admin and user. The admin can modify the webpages and add new menu items. 
The user can reset password and order from the menu.
